#? stdtmpl | standard
#proc generate_jsondoc_items_page(jd_items: JsonDocPkgItems): string =
#  result = ""

<div class="columns">
<div class="column col-12">
	<h4>${jd_items.len} entries found</h4>
</div>

#for pname, item in jd_items:
	<div class="column col-12">
		<div class="panel package">
			<div class="panel-header">
        <div class=""><code>${item.code}</code></div>
				<div class="">Desc: ${item.desc}</div>
				<div class="">Type: ${item.itype}</div>
				<div class="">File: ${item.filepath}</div>
				<div class="panel-title">Package: <a href="/pkg/${pname}">${pname}</a> </div>
			</div>
			<div class="panel-footer">
        Line: ${item.line} Col: ${item.col}
			</div>
      <a href="https://github.com/FedericoCeratto/nim-libsodium/blob/master${item.filepath}#L${item.line}"
          class="btn input-group-btn">
					<i class="fa fa-github"></i>View on GitHub
				</a>
      <button class="btn btn-link">link button</button>
		</div>
	</div>
#end for
